/**
 * Client-safe constants shared by the lead forms and the server gate.
 * Keep this file free of Node imports — it ships in the browser bundle.
 */

/** Name of the hidden honeypot field. Deliberately looks like a real field. */
export const HONEYPOT_FIELD = "company_website";
