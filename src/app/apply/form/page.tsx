import { Suspense } from "react";
import ApplyFormLanding from "@/components/apply/ApplyFormLanding";

export default function ApplyFormPage() {
  return (
    <Suspense>
      <ApplyFormLanding />
    </Suspense>
  );
}
