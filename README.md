# Apply VSL — speed cut (live)

Do **not** merge this branch. It is only here so Buzz can grab the production video.

- **File:** `vsl-speed.mp4` (~25MB, intro → demo)
- **Serve at:** `https://quotie.au/apply/vsl-speed.mp4`
- **Drop into:** `public/apply/vsl-speed.mp4` on the production deploy (that path is gitignored on `main` / `benji/apply-page`)

The player on `/apply` requests `/apply/vsl-speed.mp4?v=2`.

Do not use `vsl-belief.mp4` or `vsl.mp4`.
